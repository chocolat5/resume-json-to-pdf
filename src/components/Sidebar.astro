<aside class="sidebar">
  <slot />
</aside>

<style lang="scss">
  .sidebar {
    display: flex;
    flex-direction: column;
    gap: var(--sp-lg);
    padding: var(--sp-3xl) var(--sp-xl) var(--sp-3xl) var(--sp-2xl);
    background-color: var(--theme-accent);
  }
</style>
